function solution(number, k) {
    let answer = '';
    
    answer = 'a';

    // answer = number.split('').sort((a, b) => b - a).join('').slice(0, number.length - k);
    
    
    return answer;
}

console.log(solution("1924", 2));